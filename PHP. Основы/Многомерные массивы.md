Если мы хотим сохранить несколько массивов внутри переменной - 

$persons = [
[1 Весь массив переносим ( их ключ с переменной)]
[2 Весь массив переносим ( их ключ с переменной)]
[3 Весь массив переносим ( их ключ с переменной)]
];

Выводится вот так -

foreach($persons as $person) {
	echo print_r($person);

Если просто имена  то 

foreach($persons as $person) {
	echo print_r($person['name']);
	