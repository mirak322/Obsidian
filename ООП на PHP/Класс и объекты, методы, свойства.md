	Можем хрнаить как и статичные($sum = 5+ 10);
значение так и динамичные function matematika($a, $b);

Все переменные статичные - свойства
А все функции - методы



class Worker
{
//Фиксированные значения
// строки
// числа / флот (с плав точкой)
// булевые числа
// null

==**Свойства**==

**public string $name;**
**public int $age;**
**public array $hours;**


// Массивы

**==Методы==**

public function work()
{
	print_r('in working');
}
//Динамичные значения
}


require_once('../app/Worker.php');

$worker = new Worker

new - создаем объект
Worker - Имя класса

$worker->work();
//->work - вызов метода 

или же
создаем фалик
composter.json

{
	"autoload": {
		"psr-4": {
			"App\\": "app/"
		}

}
}

после скачиваем composer
создает папку vendor

после пишем

require_once ('../vendor/autoloar.php');

В класс воркер пишем путь к классу

namespace App;


