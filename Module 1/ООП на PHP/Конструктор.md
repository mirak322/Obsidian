$worker = new \App\Worker();

Удобный способ дать что такое имя, возраст, объект

public function **(2 нижн поддчеркивания)** construct(name)
{
	$this->name = $name;
	$this->age = $age;
	$this->hours = $hours;
}

Конструктор - это метод у классов, можем прокинуть аргументы туда

$this - это обращение к будущему объекту который будет создан на основе класса
